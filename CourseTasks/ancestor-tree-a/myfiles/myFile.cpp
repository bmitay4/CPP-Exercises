#include <iostream>
using namespace std;

typedef struct node {
    int value;
    node* next;
} node;
int main() {
    /*node* n1 = new node;
    node* n2 = new node;
    n2->value = 100;
    n1->value = 10;
    n1->next = n2;
    n2 = nullptr;

    if (n1->next != nullptr)
        cout << n1->next->value << endl;

    if (n2 == nullptr)
        cout << "success" << endl;

    node* n3 = new node;
    n3->next = n1->next;
    n3->next = nullptr;

    if (n1->next != nullptr)
        cout << n1->next->value << endl;

    n1->next = nullptr;

    if (n1->next == nullptr)
        cout << "success" << endl;
*/
    return 0;
}